# 작업 로그 — 2026-05-29

## 텍스트 선택 개선 작업 (진행중)

### 수정한 파일

| 파일 | 변경 |
|------|------|
| `frontend/src/App.js` | 258 insertions, 125 deletions |
| `frontend/src/styles.css` | 16 insertions, 1 deletion |

---

### 수정 내용 요약

#### 1. 텍스트 선택 판정 로직 재구현 (`isViewportRectSelected`)

- centerX/centerY 기반 조건 제거
- `getIntersectionArea()` fallback 제거
- `TEXT_SELECTION_MIN_OVERLAP_RATIO` 상수 제거
- **새로운 방식:** verticalOverlapRatio + horizontalOverlapRatio 기반 단순화
  - `verticalOverlapRatio < 0.45` → early reject
  - `candidateSize.width <= 14` (좁은 token): `horizontalOverlapRatio >= 0.18`
  - 기본: `horizontalOverlapRatio >= 0.35`

#### 2. 한글 token 분리 시도 → 복구

- **초기:** 한글을 단어 단위(`[가-힣]+`)에서 글자 단위(`[가-힣]`)로 분리
  - 문제: "심수관" 옆 "주가고시마"가 함께 선택됨
- **해결 시도 1:** `isViewportRectSelected()` 조건 강화 (수평 겹침 비율 추가)
- **해결 시도 2:** `centerInside` 조건에 `horizontalOverlapRatio >= 0.45` 추가
- **해결 시도 3:** `TEXT_SELECTION_MIN_OVERLAP_RATIO` 0.08 → 0.22 상향
- **결론:** 한글 글자 단위 token이 overlap 판정과 충돌하여 선택 불안정 발생
- **최종:** 한글을 다시 단어 단위(`[가-힣]+`)로 복구
- `isViewportRectSelected()` 단순화된 overlap 기반 로직 유지

#### 3. redactionRect 높이 계산 수정 (`makeMergedTextViewportRect`)

- 이전: `centerY ± visualHeight/2` — 평균/재구성으로 인한 오차 발생
- 변경: `rawRect.y0/y1` 직접 사용 — 실제 텍스트 bounding box 추종
- 패딩 상수 조정:
  - `TEXT_SELECTION_VERTICAL_PADDING`: 0.14 → 0.06
  - `TEXT_REDACTION_VERTICAL_PADDING`: 0.08 → 0.02
  - `TEXT_SELECTION_ANCHOR_TOLERANCE`: 2 → 4

#### 4. textItems 상태 관리 변경

- `useState` → `useRef` (`textItemsRef`)로 변경
- 드래그 중 불필요한 리렌더링 방지

#### 5. 텍스트 레이어 및 CSS 정리

- `.text-layer` 및 `.text-layer span` → `display: none`
- `.page-stage.is-text-mode.is-text-hover` 클래스 제거
- z-index 계층 정리 (canvas: 0, preview: 2, redaction: 3, selected: 4)

#### 6. 디버그 로그 (DEV 전용, 유지 중)

- `[SELECTED_SEGMENTS_DEBUG]` — `mergeSelectedTextSegments()`에서 선택된 segment 목록
- `[TEXT_SELECTION_DEBUG]` — 드래그 종료 시 선택/redaction 개수
- `[TEXT_SELECTION_RECT_DEBUG]` — display/redaction rect 상세 좌표

#### 7. 제거된 로그

- `[ITEM_HEIGHT_DEBUG]` — getTextItemViewportRect()에서 제거
- `[TOKEN_SELECTION_DEBUG]` — collectSelectedTextSegments()에서 제거 (드래그 중 콘솔 폭주)

---

### 해결된 문제

- redactionRect 높이가 단어마다 불규칙하던 문제 수정
- "심수관" 옆 "주가고시마"가 함께 선택되던 문제 완화 (overlap 비율 강화)
- 드래그 중 콘솔 로그 폭주 제거
- `isTextHover` 상태 및 관련 CSS 불필요 코드 제거

### 남아있는 문제

- 한글 텍스트 선택이 여전히 불안정할 가능성 있음
  - "심수관" 드래그 시 "주가고시마"까지 선택되는 문제가 완전히 해결되지 않음
  - 근본 원인: 한글 token이 단어 단위로 묶여 있어, 드래그 영역이 일부 걸리면 token 전체가 선택됨
- 텍스트 선택 미스 시 사용자 피드백 부족 (onTextSelectionMiss 제거됨)
- 텍스트 레이어가 `display: none` 상태 — 브라우저 네이티브 선택 활용 필요

### 다음 작업 시 확인할 사항

1. `isViewportRectSelected()`의 horizontalOverlapRatio 임계값(0.18 / 0.35) 추가 튜닝 필요 여부
2. 한글 token을 단어 단위로 유지할지, 다른 접근 방식(예: 선택 영역을 글자 단위로 clip)을 고려할지 결정
3. `[SELECTED_SEGMENTS_DEBUG]`, `[TEXT_SELECTION_DEBUG]`, `[TEXT_SELECTION_RECT_DEBUG]` 로그 제거 시점
4. `styles.css`의 `.text-layer` display:none 처리 — 추후 native textLayer로 전환할지 결정
5. 텍스트 선택 실패 시 사용자에게 시각적 피드백을 줄 방법 고려
